server:
  port: ${SERVER_PORT:8083}
  name: ${SERVER_NAME:localhost}

# Standalone mode - disable distributed features
distributed:
  enabled: false
  
ping:
  server:
    enabled: false

spring:
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:dueling_db}
    username: ${POSTGRES_USER:-user}
    password: ${POSTGRES_PASSWORD:-password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      use-second-level-cache: true
      use-query-cache: true
      cache:
        use-minimal-puts: true
        region.factory_class: org.redisson.hibernate.RedissonRegionFactory
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        cache.redisson.instance_name: redissonClient 

jwt:
  secret: ${JWT_SECRET:mySecretKeyForDuelingProtocolThatShouldBeLongerThan256Bits}
  expiration: ${JWT_EXPIRATION:86400000}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    tags:
      application: dueling-protocol

logging:
  level:
    root: WARN
    # Silenciar frameworks
    org.springframework: WARN
    org.hibernate: WARN
    org.apache: WARN
    io.lettuce: WARN
    io.netty: WARN
    com.zaxxer.hikari: WARN
    org.postgresql: WARN
    # Silenciar avisos de modo standalone
    ping: ERROR
    service.registry: ERROR
    service.election: INFO
    # Seus logs da aplicação
    api: INFO
    config: INFO
    controller: INFO
    dto: INFO
    exception: INFO
    mapper: INFO
    model: INFO
    pubsub: INFO
    repository: INFO
    security: INFO
    service: INFO
    websocket: INFO

client:
  default-host: ${SERVER_HOST:localhost}
  default-port: ${SERVER_PORT:8083}